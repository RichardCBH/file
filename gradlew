#!/usr/bin/env sh

# This is a placeholder. The CI workflow uses the 'gradle' command provided by gradle/actions/setup-gradle.
# For local development, run: gradle wrapper --gradle-version 8.7

echo "Please use 'gradle' command or generate proper wrapper with: gradle wrapper --gradle-version 8.7"
exit 0
